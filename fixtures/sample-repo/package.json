{
  "name": "maintainer-sample-repo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test ./test/checkout.test.mjs",
    "lint": "node -e \"process.exit(0)\"",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "stripe": "21.0.1",
    "openai": "4.104.0",
    "@anthropic-ai/sdk": "0.39.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
